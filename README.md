# Contact-Management-System
A modular C++ application replacing manual workflows with efficient digital directory management. Implements file-based persistence (fstream), secure input validation, and structured CRUD operations using dynamic vector storage. Optimized for usability and error handling, with an architecture designed for scaling.
